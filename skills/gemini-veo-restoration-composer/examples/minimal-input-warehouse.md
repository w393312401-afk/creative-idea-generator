# Minimal Input — Warehouse Restoration

## Demonstrates

Tier 1 input → full prompt set. The user provides only one sentence. The composer auto-infers all parameters, builds the drift-lock packet, derives 3 production beats, and renders IMAGE 1-4 + VIDEO 1-3.

## User Input

```text
做一个废弃仓库翻新
```

## Internal Inference (not shown to user)

```
CARRIER: abandoned industrial warehouse
ENV: industrial zone, interior bay
TRAUMA: collapsed insulation, rust-streaked shelving, broken glass, damp concrete, blown plaster
DESTINY: restrained functional industrial shell
REWARD ACTION: final carry-out + practical light stabilization (subtle convergence)
Space Type: abandoned property
Mode: Standard
N: 3 (hazard clearing → shell repair → final carry-out and practical stabilization)
Camera: static tripod, ultra-wide 14-18mm, 1.6m, eye-level, diagonal full-bay
Lighting: soft overcast daytime continuity → ambient only → ambient only → temporary work light → final practical
```

## Composed Output

> The output below is identical in quality and structure to a direct `restoration-timelapse-engine` invocation. See the canonical reference at:
> `/Users/fly/.codex/skills/restoration-timelapse-engine/examples/abandoned-warehouse-auto-defaults.md`

This example validates that:
1. A single-sentence input produces a complete, contract-compliant prompt set.
2. Camera DNA Block is copied character-for-character across IMAGE 2-4.
3. IMAGE 1 carries the full landmark and boundary enumeration.
4. IMAGE 2+ uses the inheritance clause with 2-3 drift-prone items.
5. Every VIDEO has the fixed anchor-first opening + adjacent-frame binding.
6. Object-ledger phrases (yellow fiberglass ladder, black tool cases) are copied literally.
7. Passive environmental cues are present with per-clip variation.
8. Audio matches visible action per phase.
9. The final VIDEO uses carry-out convergence, not a full reward reveal.
