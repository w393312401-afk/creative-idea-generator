interface:
  display_name: "修复提示词作曲家"
  short_description: "一键从主题描述生成完整的 IMAGE/VIDEO 延时改造提示词集合"
  default_prompt: "Use $gemini-veo-restoration-composer to turn this topic description into a complete, copy-ready IMAGE/VIDEO prompt set for a restoration/renovation time-lapse. Accept minimal input (even one sentence), auto-build the Drift Lock Packet, and derive the production beat ladder from the space type. Follow the skill's Staged Delivery Contract: compose IMAGE 1 only, run scripts/render_and_gate_anchor.py synchronously and wait for it to exit 0, show the user the rendered anchor and say plainly that nothing judged it automatically, and only then compose the remaining IMAGE anchors and anchor-first VIDEO prompts — never emit the full prompt set in one pass unless the user explicitly asks for text-only output or the render server at http://127.0.0.1:8085 is unreachable. Run silent self-check gates, deliver in the standard fenced text block with Chinese labels, then auto-save to the local idea library via scripts/save_to_library.py."
