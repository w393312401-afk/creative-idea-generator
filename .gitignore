# 私密:服务端模型密钥与访问码,绝不提交
server_config.json

# 运行产物 / 缓存
__pycache__/
*.pyc
server.log
server_stdout.log
server_stderr.log

# 用户数据(按需,默认也不提交)
library.json
outputs/

# 新增忽略规则
tasks.json
packet_cache.json
scratch/
image-service-station/restored_history.json

# System / Temporary files
out.txt
server.pid
stderr_crash.txt
process_brief_cache.json
*.log
.claude/
.gemini/

