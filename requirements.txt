# SPARK 创意点子工坊 — Python 依赖清单
# 安装: pip install -r requirements.txt
# Google FX 浏览器运行时已内置，依赖也统一由本文件管理。

# 图像处理：帧序列 WebP 转换、LAB 颜色匹配、封面合成（硬依赖，缺失无法启动）
Pillow>=10.0

# 数值计算：本地视觉质量探针的唯一依赖——下载视频的首尾帧锚点比对（防串片/文生视频
# 混入成片）、i2v 提交前的帧对契约、锚点惰性检测。这些探针缺 numpy 时会静默返回
# 'skipped' 而不报错（设计如此，探针不该拖垮主流程），后果是整套内容级校验悄悄失效
# 而日志上看不出异常——所以必须显式声明，别让它靠"碰巧装过"活着。
numpy>=1.24

# HTTP 客户端：/api/generate_image_with_vlm 的上游调用使用 requests，
# 其余出站请求走标准库 urllib
requests>=2.31

# Google FX UI 自动化：通过 AdsPower 提供的 CDP 地址连接已安装浏览器。
playwright>=1.45
pydantic>=2.7,<3
python-dotenv>=1.0
